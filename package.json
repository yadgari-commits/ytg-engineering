{
  "name": "ytg-engineering",
  "version": "4.0.0",
  "description": "YTG Engineering — Offline engineering calculation center. Yadgari Trading Group.",
  "private": true,
  "scripts": {
    "build:android": "cap sync android && cd android && gradlew assembleDebug",
    "init:android": "cap init \"YTG Engineering\" com.ytg.engineering --web-dir . && cap add android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
